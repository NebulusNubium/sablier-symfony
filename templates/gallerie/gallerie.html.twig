{% extends 'base.html.twig' %}

{% block title %}Gallerie{% endblock %}

{% block main %}

<section id="liste">
    <div class="gallerie-title">
        <h1>Notre Collection</h1>
        <p>chaque objet a sa page détaillée en cliquant dessus</p>
        <p>bonus: faire un filtre et un systeme de notation</p>
        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <a href="{{path('addPicture')}}" class="btn-secondary">Ajouter un sablier</a>
        {% else %}
            <a href="{{ path('app_login') }}" class="btn-secondary">Se connecter</a>
        {% endif %}
    </div>
    <div class="cards">
    {% for picture in pictures %}
        <div class="card">
            <h2>{{ picture.nom }}</h2>
            <p><strong>Ajouté par :</strong> {{ picture.user.username}}</p>
            <p><strong>Description :</strong> {{ picture.description }}</p>
            {# <p><strong>Les tags :</strong>
                {% for tag in picture.tags %}
                    {{ topping.toppingType }}{% if not loop.last %}, {% endif %}
                {% else %}
                    Aucun
                {% endfor %} #}
            </p>
            <div class="image">
                <a class="img" href="{{path('detail',{'id': picture.id}) }}"><img src="{{ asset('uploads/images/' ~ picture.imageName) }}" alt="img"></a>
            </div>
            {# <div class="actions">
                <a class="modifier" href="{{ path('updateDessert', {'id': picture.id}) }}">Modifier</a>
                <form action="{{ path('deleteDessert', {'id': picture.id}) }}" method="post" onsubmit="return confirm('Confirmer la suppression?')">
                    <input type="hidden" name="_method" value="delete">
                    <input type="hidden" name="_token" value="{{ csrf_token('SUP' ~ picture.id) }}">
                    <input type="submit" value="Supprimer" class="supprimer">
                </form>
            </div> #}
        </div>
    {% endfor %}
    </div>
</section>

{% endblock %}
